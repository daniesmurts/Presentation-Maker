# Copy to deploy/.env.deploy (gitignored). Read by deploy/deploy.sh; an
# exported variable in the shell overrides a value here.
VM_HOST=deploy@<static IP of the VM>
DOMAIN=tezarium.ru
IMAGE_REPO=cr.yandex/<registry id>/tezarium
# APP_DIR=/opt/tezarium
